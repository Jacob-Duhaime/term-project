#include <string>

class Spellcheck{
    private:
        // private parts
        std::string word;
    public:
        // public parts
        Spellcheck(std::string user_input); //constructor
        struct Node();
        void insert(Node, string s);
        
};
